class CommandToine:

    def prend_objet(self, s, p, mess):
        if (p.getVerbose()):
            print(rec)
        var = 'prend object\n'
        mess.sendMessage(s, var)

    def pose_objet(self, s, p, mess):
        if (p.getVerbose()):
            print(rec)
        var = 'pose object\n'
        mess.sendMessage(s, var)
